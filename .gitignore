# R specific
.Rproj.user
.Rhistory
.RData
.Ruserdata
*.Rproj

# Data files (we'll add data separately)
*.csv
data/*.csv
!data/README.md

# Large output files
*.rds
output/*.rds

# System files
.DS_Store
Thumbs.db
*~

# Temporary files
*.tmp
*.log
```

**Save the file** (Ctrl+S / Cmd+S)

---

### **4.4: Commit and Push Your Changes**

**Now the magic happens!**

**Look at the Git tab** (top-right pane in RStudio):

You should see files listed with **yellow "?"** icons (untracked files).

**Step-by-step:**

1. **Check the boxes** next to:
   - `.gitignore`
   - `code/` folder (all files in it)
   - `data/README.md`
   - `output/README.md`
   - `figures/README.md`
   - `reports/README.md`

2. **Click "Commit"** button

3. **A new window opens.** In the "Commit message" box (top-right), type:
```
   Initial project setup with folder structure